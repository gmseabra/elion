# ── web app (required) ──
flask>=3.0
pyyaml>=6.0
numpy>=1.24
requests>=2.28

# ── chemistry / modeling (required for the ChemBERT + docking views) ──
rdkit>=2023.3            # cheminformatics (import name: rdkit)
torch>=2.1              # ChemBERT + DeepAtom saliency
scikit-learn>=1.3       # metrics / preprocessing in finetuning
pandas>=2.0
tqdm>=4.65

# ── UI action router (RAG over the action KB) ──
faiss-cpu>=1.7          # cosine index; falls back gracefully if absent
sentence-transformers>=2.2

# ── Qwen inference server (uiapp/llm/qwen_server.py — optional, separate process) ──
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0
llama-cpp-python>=0.2   # CUDA build recommended for GPU offload

# ── plotting (RL pipeline / distribution-shift charts) ──
matplotlib>=3.7
seaborn>=0.12

# ── external, NOT on PyPI (provide via PYTHONPATH if you use those routes) ──
#   shufflenet_v3   — ships with the external DeepAtom project (deepatom_saliency)

# ── added with the Thompson-Sampling monitor + pose tooling ──────────────────
psutil>=5.9          # ts_routes: CPU/GPU monitor SSE endpoints
